<div class="page-title-container">
  <h5 class="page-title">Home</h5>
</div>

<%= render 'tweets/new' %>

<% @tweets.each do |post| %>
  <%= link_to tweet_path(post) do %>
  <div>
    <h5><%= post.title %></h5>
    <p><%= post.body %></p>
  </div>
<% end %>
<% end %>